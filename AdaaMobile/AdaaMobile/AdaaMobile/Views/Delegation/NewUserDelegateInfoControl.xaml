<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AdaaMobile.Views.Delegation.NewUserDelegateInfoControl"
             BackgroundColor="Transparent"
             x:Name="ParentView"
             >
  <Grid ColumnSpacing="8" Padding="2,8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    
    <Label Grid.Row="0" Grid.Column="0" TextColor="Black" FontSize="Medium" Text="{Binding Title,Source={x:Reference ParentView}}" HorizontalTextAlignment="Start"/>
    <Label Grid.Row="1" Grid.Column="0" LineBreakMode="TailTruncation"  TextColor="{StaticResource AppBackgroundNormal}" FontSize="Medium" FontAttributes="Bold" Text="{Binding Value,Source={x:Reference ParentView}}" HorizontalTextAlignment="Start"/>

  </Grid>
</ContentView>