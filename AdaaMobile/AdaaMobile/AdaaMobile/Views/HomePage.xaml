<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:AdaaMobile.Controls;assembly=AdaaMobile"
             x:Class="AdaaMobile.Views.HomePage">
    <Grid>
        <Label Text="HomeView" VerticalOptions="Center" HorizontalOptions="Center"  />
        <controls:ColumnsPanel ColumnCount="2"
                             ItemSource="{Binding Pages}"
                             ColumnWidth="150"
                             ColumnHeight="150"
                             >
            <controls:ColumnsPanel.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <BoxView VerticalOptions="Fill" 
                                 HorizontalOptions="Fill"
                                 BackgroundColor="#FF007C84"
                                 >
                            <Label Text="{Binding Title}" VerticalOptions="Fill" HorizontalOptions="Fill"/>
                        </BoxView>
                    </Grid>
                </DataTemplate>
            </controls:ColumnsPanel.ItemTemplate>

        </controls:ColumnsPanel>
    </Grid>
</ContentPage>