<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AdaaMobile.Views.DirectoryPage"
             xmlns:controls="clr-namespace:AdaaMobile.Controls;assembly=AdaaMobile"
             BackgroundColor="White"
             >
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"></RowDefinition>
    </Grid.RowDefinitions>
    <!--IsGroupingEnabled="True"
              GroupDisplayBinding="{Binding Key}"
              GroupShortNameBinding="{Binding Key}"-->
    <ListView
          ItemsSource="{Binding GroupedEmployees}"
          HasUnevenRows="true"
          IsGroupingEnabled="True"
          GroupDisplayBinding="{Binding Key}"
          GroupShortNameBinding="{Binding Key}"
              >
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell >
            <ViewCell.View>
              <Grid Padding="15">
                <controls:ExtendedGrid VerticalOptions="Fill"
              HorizontalOptions="Fill"
              CornerRadius="5"
              BackgroundColor="Transparent"
                      >
                  
                  <Label HorizontalOptions="Fill" VerticalOptions="Fill" BackgroundColor="{StaticResource AppBackgroundNormal}" TextColor="White" Text="{Binding Name}"></Label>
                </controls:ExtendedGrid>
              </Grid>
            </ViewCell.View>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</ContentPage>